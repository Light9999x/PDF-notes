# 專案協作偏好

- 使用繁體中文溝通。
- 使用者已要求：以後不要自動幫他執行 App。除非使用者明確要求啟動，勿啟動 App、開發／預覽伺服器、或自動開啟 App 瀏覽器畫面；請提供快速開啟與停止指令。
- 上述偏好亦適用於會啟動網站服務的瀏覽器／啟停整合測試。可以進行不啟動服務的靜態檢查、單元測試、建置與文件更新；需要啟動服務的測試標示未執行即可，不為此反覆詢問確認。
- 快速指令（專案根目錄 PowerShell）：`./app/start-pdfnote.ps1`、`./app/stop-pdfnote.ps1`。在 app/ 可使用 `npm run preview`、`npm run stop`。
- 停止伺服器與關閉離線 PWA 視窗是兩件事，不清除網站資料，不以全面結束 Node 程序處理。
